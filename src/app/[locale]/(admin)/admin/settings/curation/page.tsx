import { CurationDashboard } from "@/components/admin/settings/curation-dashboard";

export default function CurationPage() {
    return (
        <div className="container mx-auto py-10">
            <CurationDashboard />
        </div>
    );
}
